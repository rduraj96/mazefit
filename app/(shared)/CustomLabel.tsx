import { LabelProps } from "recharts";
import { ContentType } from "recharts/types/component/Label";

const CustomLabel = ({ x, y, position }: LabelProps) => {
  return <div>Poop</div>;
};

export default CustomLabel;
